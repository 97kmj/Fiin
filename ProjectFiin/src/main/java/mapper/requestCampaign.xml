<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="mapper.requestCampaign">
	<!-- 준영 -->
	<select id="selectRequest" parameterType="Map" resultType="Integer">
		select request_num from request_campaign where campaign_num =#{campaignNum} and influencer_num =#{influencerNum}
	</select>
	<insert id="insertRequest" parameterType="Map">
		<![CDATA[
			insert into request_campaign (campaign_num,influencer_num) values (#{campaignNum},#{influencerNum})
		]]>	
	</insert> 
	
	<delete id="deleteRequest" parameterType="Map">
		<![CDATA[
			delete from request_campaign where campaign_num =#{campaignNum} and influencer_num =#{influencerNum} 
		]]>	
	</delete>
</mapper>

